{
  "name": "cardapio-digital-caragua",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp src/* dist/src/",
    "start": "npx http-server -c-1 -p 5173",
    "preview": "node ./scripts/preview.js",
    "deploy": "echo \"Deploy via GH Pages configured in workflows\"",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}